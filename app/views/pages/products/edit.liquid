---
slug: products/edit
---
{% liquid
  graphql products = 'product', id: context.params.slug3
  assign product = products | dig: 'models', 'results' | first
  assign changeset = '{ "errors": {}, "payload": {}, "model": null }' | parse_json
  assign changeset = changeset | add_hash_key: 'model', product
%}
{% include 'theme/products/edit', changeset: changeset %}

{% liquid
  assign changeset = changeset | add_hash_key: 'payload', params.product
  render 'logic/products/validate', changeset: changeset
  render 'logic/shared/insert', changeset: changeset, model_schema_name: "car"
%}

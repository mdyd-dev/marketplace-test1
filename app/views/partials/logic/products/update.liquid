{% liquid
  assign changeset = changeset | add_hash_key: 'payload', params.product
  include 'logic/products/validate', changeset: changeset
  include 'logic/shared/update', changeset: changeset, model_schema_name: "car"
%}
